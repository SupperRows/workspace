<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.open.capacity.user.dao.SwipperDao">

    <resultMap type="com.open.capacity.user.model.Swipper" id="swipperMap">
        <result property="id" column="id"/>
        <result property="type" column="type"/>
        <result property="url" column="url"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="save" parameterType="com.open.capacity.user.model.Swipper">
        insert into swipper(id,type,url,create_time)
        values (#{ id},#{ type},#{ url},now())
    </insert>

    <update id="update" parameterType="com.open.capacity.user.model.Swipper">
        update swipper
        <set>
            <if test="id != null">
                id = #{id},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="url != null">
                url = #{url},
            </if>
            update_time = now(),
        </set>
        where id = #{id}
    </update>

    <delete id="delete" parameterType="long" flushCache="true">
        delete from swipper where id = #{id}
    </delete>

    <select id="findAll" parameterType="map" resultType="com.open.capacity.user.model.Swipper">
        select * from swipper t
        <include refid="where"/>
    </select>
    <sql id="where">
        <where>
            <if test="searchKey != null and searchKey != '' and searchKey=='id'">
                and t.id like concat('%', #{searchValue}, '%')
            </if>
            <if test="searchKey != null and searchKey != '' and searchKey=='type'">
                and t.type like concat('%', #{searchValue}, '%')
            </if>
            <if test="searchKey != null and searchKey != '' and searchKey=='url'">
                and t.url like concat('%', #{searchValue}, '%')
            </if>
        </where>
    </sql>
</mapper>